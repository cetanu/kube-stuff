apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kubevela-traits
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/cetanu/kube-stuff.git'
    targetRevision: HEAD
    path: k8s/infra/kubevela-traits
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: vela-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
